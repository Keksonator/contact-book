# Контактная книга (Contact Book)

Простое консольное приложение для управления контактами, написанное на Python с использованием архитектуры репозиториев и сервисов.

## 📖 Описание
Эта программа позволяет управлять вашей контактной книгой прямо в терминале. Все данные автоматически сохраняются в JSON-файл. Проект реализован с соблюдением принципов чистой архитектуры.

## 🏗️ Архитектура проекта
Проект использует многоуровневую архитектуру:

- Модели (Models) - классы данных для контактов
- Репозитории (Repositories) - работа с хранилищем данных
- Сервисы (Services) - бизнес-логика приложения
- UI (User Interface) - консольный интерфейс
## 🚀 Быстрый старт

### Предварительные требования
- Python 3.6 или выше

### Установка и запуск
1. Клонируйте репозиторий:`contact_book.py`
```bash 
git clone <repository-url>
cd contact-book
```
2. Создайте виртуальное окружение и активируйте его:
```bash
python -m venv venv
source venv/bin/activate  # Linux/MacOS
venv\Scripts\activate     # Windows
```
3. Установите зависимости:
```bash
pip install -r requirements/base.txt
```
4. Запустите приложение:
```bash
python -m src.main
```
## 🎮 Использование

После запуска вы увидите меню:
```text
==============================
1. Добавить контакт
2. Показать все контакты
3. Поиск контакта
4. Удалить контакт
5. Изменить контакт
6. Выйти
==============================
```
Просто введите цифру от 1 до 6 для выбора действия.

## 💾 Хранение данных

Данные автоматически сохраняются в файл `data/contacts_json.json` в формате JSON.

## 🛠️ Технологии

- **Язык программирования:** Python 3
- **Хранение данных:** JSON
- **Архитектура:** Многоуровневая (Models-Repositories-Services-UI)
- **Интерфейс:** Консоль (CLI)

## 📁 Структура проекта
```text
contact-book/
├── data/
│   └── contacts_json.json
├── src/
│   ├── models/
│   │   ├── __init__.py
│   │   └── contact.py
│   ├── repositories/
│   │   ├── __init__.py
│   │   └── contact_repository.py
│   ├── services/
│   │   ├── __init__.py
│   │   └── contact_service.py
│   └── ui/
│       ├── __init__.py
│       └── console_ui.py
├── tests/
│   ├── integration/
│   │    └── __init__.py
│   └── unit/
│        └── __init__.py
├── requirements/
│   ├── base.txt
│   └── test.txt
├── venv/
├──README.md
└── main.py
```
## 🎯 Функциональность

- **Добавить контакт** - создание новой записи
- **Показать все контакты** - вывод всего списка
- **Поиск контакта** - поиск по имени, телефону или email
- **Удалить контакт** - удаление по ID
- **Изменить контакт** - обновление данных существующего контакта
- **Выйти** - корректное завершение программы

**Примечание:**  Это учебный проект, созданный для демонстрации навыков программирования на Python и применения принципов чистой архитектуры.